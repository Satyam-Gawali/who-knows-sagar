import 'package:flutter/material.dart';

class AppColors {
AppColors._();

// ---------------------------------------------------------------------------
// Brand Colors
// ---------------------------------------------------------------------------

static const primary = Color(0xFFF4B400);

static const secondary = Color(0xFFFF8F00);

static const tertiary = Color(0xFFFFF3CD);

static const success = Color(0xFF2E7D32);

static const error = Color(0xFFC62828);

static const background = Color(0xFFFFFDF7);

static const text = Color(0xFF212121);

static const subText = Color(0xFF616161);

// ---------------------------------------------------------------------------
// Primary
// ---------------------------------------------------------------------------

static const onPrimary = Color(0xFF212121);

static const primaryContainer = Color(0xFFFFE082);

static const onPrimaryContainer = Color(0xFF212121);

static const inversePrimary = Color(0xFFFFCA28);

// ---------------------------------------------------------------------------
// Secondary
// ---------------------------------------------------------------------------

static const onSecondary = Color(0xFFFFFFFF);

static const secondaryContainer = Color(0xFFFFE0B2);

static const onSecondaryContainer = Color(0xFF212121);

// ---------------------------------------------------------------------------
// Tertiary
// ---------------------------------------------------------------------------

static const onTertiary = Color(0xFF212121);

static const tertiaryContainer = Color(0xFFFFF8E1);

static const onTertiaryContainer = Color(0xFF212121);

// ---------------------------------------------------------------------------
// Surface
// ---------------------------------------------------------------------------

static const surface = Color(0xFFFFFFFF);

static const onSurface = Color(0xFF212121);

static const surfaceDim = Color(0xFFF7F7F7);

static const surfaceBright = Color(0xFFFFFFFF);

static const surfaceTint = primary;

static const surfaceContainerLowest = Color(0xFFFFFFFF);

static const surfaceContainerLow = Color(0xFFFFFBF2);

static const surfaceContainer = Color(0xFFFFF7EC);

static const surfaceContainerHigh = Color(0xFFFFF2E2);

static const surfaceContainerHighest = Color(0xFFFFEDD8);

static const surfaceVariant = Color(0xFFFFF7E8);

static const onSurfaceVariant = Color(0xFF616161);

// ---------------------------------------------------------------------------
// Background
// ---------------------------------------------------------------------------

static const onBackground = text;

// ---------------------------------------------------------------------------
// Outline
// ---------------------------------------------------------------------------

static const outline = Color(0xFFD6D6D6);

static const outlineVariant = Color(0xFFECECEC);

// ---------------------------------------------------------------------------
// Error
// ---------------------------------------------------------------------------

static const onError = Color(0xFFFFFFFF);

static const errorContainer = Color(0xFFFFEBEE);

static const onErrorContainer = Color(0xFFB71C1C);

// ---------------------------------------------------------------------------
// Inverse
// ---------------------------------------------------------------------------

static const inverseSurface = Color(0xFF212121);

static const inverseOnSurface = Color(0xFFFFFFFF);

// ---------------------------------------------------------------------------
// Fixed
// ---------------------------------------------------------------------------

static const primaryFixed = Color(0xFFF4B400);

static const primaryFixedDim = Color(0xFFFFCA28);

static const onPrimaryFixed = Color(0xFF212121);

static const onPrimaryFixedVariant = Color(0xFF424242);

static const secondaryFixed = Color(0xFFFF8F00);

static const secondaryFixedDim = Color(0xFFFFB74D);

static const onSecondaryFixed = Color(0xFFFFFFFF);

static const onSecondaryFixedVariant = Color(0xFF424242);

static const tertiaryFixed = Color(0xFFFFF3CD);

static const tertiaryFixedDim = Color(0xFFFFECB3);

static const onTertiaryFixed = Color(0xFF212121);

static const onTertiaryFixedVariant = Color(0xFF424242);
}
